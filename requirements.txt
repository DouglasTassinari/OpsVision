# dataset v3 (pt-BR) — mudanças neste arquivo forçam rebuild completo no
# Streamlit Cloud; pushes só de código não reiniciam o processo em execução.
streamlit>=1.58,<2.0
SQLAlchemy>=2.0,<3.0
pandas>=3.0,<4.0
plotly>=6.0,<7.0
Faker>=40.0,<41.0

# dev / tooling
pytest>=9.0,<10.0
ruff>=0.15,<1.0
